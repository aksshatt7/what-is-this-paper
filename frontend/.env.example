# Set this to your Railway backend URL in Vercel's environment variable settings.
# Leave unset for local development (Vite's proxy handles /api → localhost:8000).
VITE_API_BASE_URL=https://your-railway-app.railway.app
